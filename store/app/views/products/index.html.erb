<!-- "." is a relative lookup based on the controller
  and action. This does "en.products.index.title"
-->
<h1><%= t ".title" %></h1>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product_path(product.id), class: "text-link" %>
    </div>
  <% end %>
</div>
