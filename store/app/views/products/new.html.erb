<h1>New product</h1>

<div style="max-width: 600px;">
  <%= render "form", product: @product %>
  <div class="actions" style="margin-top: var(--spacing-lg);">
    <%= link_to "Cancel", products_path, class: "link-button secondary" %>
  </div>
</div>
